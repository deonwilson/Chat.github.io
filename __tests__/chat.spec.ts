beforeAll(async () => {
	// Use if needed
});

describe('Create chat', () => {
	beforeEach(() => {
		// Use if needed
	});

	it('should create a new valid Chat', () => {
		// TODO
		const result = false;
		expect(result).toBeTruthy();
	});

	it('should throw error for an invalid chat format', () => {
		// TODO
		const result = false;
		expect(result).toBeTruthy();
	});
});
