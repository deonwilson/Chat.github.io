beforeAll(async () => {
	// Use if needed
});

describe('Add message to chat', () => {
	beforeEach(() => {
		// Use if needed
	});

	it('should add a new valid message', () => {
		// TODO
		const result = false;
		expect(result).toBeTruthy();
	});

	it('should throw error for an invalid message format', () => {
		// TODO
		const result = false;
		expect(result).toBeTruthy();
	});
});
